# Database - Auto-detects PostgreSQL or MySQL
spring.datasource.url=${DATABASE_URL:jdbc:mysql://localhost:3306/cafeweb}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}

# JPA/Hibernate
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.format_sql=true

# Hibernate will auto-detect the dialect based on the JDBC URL
# Or you can explicitly set it:
spring.jpa.properties.hibernate.dialect=${HIBERNATE_DIALECT:org.hibernate.dialect.MySQL8Dialect}

# Server Port - allows Render to set it
server.port=${PORT:8080}

# Email Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:deathop17@gmail.com}
spring.mail.password=${MAIL_PASSWORD:txhlqtvlykthgjal}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.connectiontimeout=30000
spring.mail.properties.mail.smtp.timeout=30000
spring.mail.properties.mail.smtp.writetimeout=30000
spring.mail.properties.mail.debug=true
logging.level.com.sun.mail=TRACE
spring.mail.properties.mail.transport.protocol=smtp
spring.mail.properties.mail.smtp.socketFactory.fallback=false
spring.mail.properties.mail.smtp.ssl.trust=*
spring.mail.properties.mail.smtp.ssl.protocols=TLSv1.2
logging.level.org.springframework.mail=DEBUG
logging.level.javax.mail=TRACE

# JWT Secret - use environment variable for production
jwt.secret=${JWT_SECRET:D7g47C8eqMaupVeMZV8RBuxAffP75/qKoIVjNJUucUg=}

# File Upload Configuration - dynamic path for production
file.upload-dir=${FILE_UPLOAD_DIR:/tmp/uploads}
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
